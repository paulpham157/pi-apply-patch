# Target Pi 0.85.1 for hybrid patch calls

Require Pi 0.85.1 or later and use its public constrainedSampling contract for the universal apply_patch tool. Use grammar where the provider capability supports it and the same tool's JSON input schema otherwise, preserving one patch engine across model families. Supporting older Pi versions would require a separate compatibility path, which is outside this version's scope.
